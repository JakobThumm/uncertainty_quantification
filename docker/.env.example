# Example environment file for Docker Compose
# Copy this file to .env and modify if needed
# By default, the build.sh and run.sh scripts automatically set these values

# User configuration (automatically detected by scripts)
# USER_ID=1000
# GROUP_ID=1000
# USERNAME=user

# Display for X11 forwarding (optional)
# DISPLAY=:0
